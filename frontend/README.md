# Issue #5 — Design System & Component Library

Frontend-only implementation of the shared UI foundation for the UTH Attendance System.

Included:
- design tokens for color, typography, spacing and surfaces
- reusable buttons, inputs, selects and six-digit code fields
- cards, tables, badges, alerts and action controls
- responsive foundation used by the later Figma screens

No backend code is changed.
